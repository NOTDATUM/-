# Biology Exchange · 13대 노트북 연결 방법

## 1. 스태프 노트북에서 서버 열기

터미널에서 프로젝트 폴더로 이동한 뒤 아래 명령을 실행합니다.

```bash
cd /Users/kim/Documents/Codex/dong
npm run server:lan
```

처음 실행할 때 macOS가 네트워크 연결 허용을 묻는다면 **허용**을 누릅니다.

## 2. 참가 조 노트북 접속

- 스태프와 1~12조 노트북을 모두 같은 Wi-Fi에 연결합니다.
- 터미널에 표시된 `http://192.168...:3000` 형태의 주소를 모든 노트북에서 엽니다.
- 스태프 노트북은 `http://localhost:3000`으로 접속해도 됩니다.
- 스태프는 `staff`, 각 조는 `1`부터 `12`까지의 아이디로 로그인합니다.

## 3. 게임 운영

- 스태프가 시드머니를 확정하면 모든 조 화면이 자동으로 거래 화면으로 바뀝니다.
- 조의 매수·매도와 스태프의 라운드 진행은 약 2초 안에 다른 노트북에 반영됩니다.
- 서버를 실행한 터미널은 게임이 끝날 때까지 닫지 않습니다.
- 화면을 새로고침하거나 서버를 재시작해도 게임 데이터는 `.lan-data` 폴더에 남습니다.
- 게임을 새로 시작하려면 스태프 화면의 **시드머니 재설정**을 사용합니다.

## 연결되지 않을 때

- 모든 노트북이 같은 Wi-Fi인지 확인합니다.
- VPN을 끄고 다시 시도합니다.
- 스태프 노트북의 macOS 방화벽에서 Node 또는 workerd의 수신 연결을 허용합니다.
- 터미널에 주소가 여러 개 표시되면 `192.168`, `10.`, 또는 `172.16~31`로 시작하는 주소를 먼저 사용합니다.
